# Deploy to DEV Function App on pull requests
# Docs: https://github.com/azure/functions-action

name: Deploy AI Email Parser (Dev)

on:
  pull_request:
    branches:
      - main
    paths:
      - "azure-functions/email-parser-ai/**"
      - ".github/workflows/main_pickleballkiosk-ai-email-dev.yml"
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_PACKAGE_PATH: "azure-functions/email-parser-ai"
  PYTHON_VERSION: "3.11"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python version
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate

      - name: Install dependencies
        run: pip install -r ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}/requirements.txt

      - name: Zip artifact for deployment
        run: |
          cd ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
          zip -r ../../release.zip . -x "*.pyc" -x "__pycache__/*" -x ".venv/*" -x "venv/*"

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app

      - name: Unzip artifact for deployment
        run: |
          unzip release.zip
          rm release.zip

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_B3FD505388D0468B9A1B41E0A12FF2F8 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_F83ADF1B0F6649AAB9945F1AB6285EA3 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_FF996D127ED747278ECCDAD358603A0A }}

      - name: Deploy to Azure Functions (Dev)
        uses: Azure/functions-action@v1
        id: deploy-to-function
        with:
          app-name: "pickleballkiosk-ai-email-dev"
          slot-name: "Production"
          package: "."

      - name: Output function URL
        run: |
          echo "## 🚀 Deployed to Dev" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Function URL: https://pickleballkiosk-ai-email-dev.azurewebsites.net/api/parse-email" >> $GITHUB_STEP_SUMMARY
